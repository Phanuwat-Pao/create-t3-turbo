// oxlint-disable-next-line unicorn/prefer-export-from -- the local binding is required for the iconWithClassName side effect below
import { X } from "lucide-react-native";

import { iconWithClassName } from "./icon-with-class-name";

iconWithClassName(X);
export { X };
